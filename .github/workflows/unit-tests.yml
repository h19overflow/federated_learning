name: Unit Tests

on:
  push:
    branches:
      - main
      - develop
      - 'feature/**'
      - 'fix/**'
  pull_request:
    branches:
      - main
      - develop

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  unit-tests:
    name: Unit Tests (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    timeout-minutes: 15

    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.12']

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest pytest-cov pytest-asyncio

      - name: Run unit tests
        env:
          # Database settings (not used in unit tests but required for imports)
          POSTGRES_DB: test_db
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          POSTGRES_PORT: 5432
          POSTGRES_DB_URI: sqlite:///:memory:
          # AI/LLM settings (not used in unit tests but required for imports)
          GEMINI_API_KEY: test_key
          GOOGLE_API_KEY: test_key
          BASE_LLM: gemini-1.5-flash
          # LangSmith settings (not used in unit tests but required for imports)
          LANGSMITH_TRACING: false
          LANGSMITH_ENDPOINT: https://api.smith.langchain.com
          LANGSMITH_API_KEY: test_key
          LANGSMITH_PROJECT: test_project
        run: |
          pytest tests/unit \
            -v \
            --tb=short \
            --maxfail=10 \
            --cov=federated_pneumonia_detection \
            --cov-report=term-missing \
            --cov-report=xml

      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.12'
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false

      - name: Print test summary
        if: always()
        run: |
          echo "Test execution completed"
          echo "Check the logs above for details"
